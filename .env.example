# Copy to .env and fill in (never commit .env)
ASTROSPHERIC_API_KEY=

# Credits (v2 monthly pool): GetForecastData ~65/pull (Cloud+Transparency+Seeing).
# Moon endpoint is a separate 10-credit call, cached ~30 days in-app.
